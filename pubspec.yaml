name: epub_pro
description: EPUB Parser for Dart. Suitable for use on the Server, the Web, or in Flutter
homepage: https://github.com/watate/epub_pro
issue_tracker: https://github.com/watate/epub_pro/issues
version: 5.4.7

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  archive: ^4.0.7
  path: ^1.9.0
  xml: ^6.5.0
  image: ^4.1.7
  collection: ^1.18.0
  word_count: ^1.0.1

dev_dependencies:
  test: ^1.25.2
  lints: ^5.1.1
