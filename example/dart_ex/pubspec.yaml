name: dart_ex
description: Example dart project
version: 1.0.0+1
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  # Epub
  epub_plus:
    path: ../../
